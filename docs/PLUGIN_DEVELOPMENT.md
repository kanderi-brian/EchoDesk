# Plugin Development Guide

Plugins use the existing `Plugin` API and are discovered from folders containing
`plugin.py`. Declare metadata and permissions, validate dependencies, and never
store credentials in plugin metadata or logs.
