# Administrator Guide

Use `ConfigurationWizard` for first-run configuration and select development,
testing, or production settings using `apply_profile`. Back up user data with
`BackupManager`; validate every archive before restore.
