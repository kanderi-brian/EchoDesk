# Troubleshooting Guide

Create a redacted support report with `Diagnostics.write("diagnostics.json",
brain)`. Check `logs/` for rotated application logs. PySide6 must be installed
to run Studio; Voice, Vision, and local LLM availability depend on local setup.
