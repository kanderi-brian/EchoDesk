# Security Overview

SecurityEngine enforces policy, permissions, approvals, credential redaction,
and audit records. Production packaging excludes secrets. Backup validation
rejects unsafe archive paths before any data is imported.
